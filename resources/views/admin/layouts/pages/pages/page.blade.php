@extends('admin.layouts.partials.dashboardApp')
@section('page_title')
    {{!is_null($currentPage->title) ? $currentPage->title : ''}} | Admin Panel
@endsection
@section('content')
    <div class="dashboard">
        <div class="container-fluid">
            <div class="row">
                @include('admin.layouts.partials.sidebar')
                <div class="col-lg-10 col-md-12 col-sm-12 col-pad">
                    <div class="content-area5">
                        <div class="dashboard-content">
                            <div class="row">
                                <div class="col-lg-12">
                                    <div class="dashboard-list">
                                        <div class="dashboard-message bdr clearfix ">
                                            <div class="tab-box-2">
                                                <div class="clearfix mb-30 comments-tr"> <span class="text-center">{{!is_null($currentPage->title) ? $currentPage->title : ''}}</span></div>
                                                {!! $currentPage->content !!}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>

@endsection
